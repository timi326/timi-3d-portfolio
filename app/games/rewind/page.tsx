import RewindGame from './rewind-game';

export const metadata = { title: '房间倒带 — Timi 的 3D 时间解谜', description: '倒带物品，留下行动回声。三个机关，一个日落前的房间。' };
export default function RewindPage() { return <RewindGame />; }
